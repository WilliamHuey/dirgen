'use strict';

/* global it, describe, before, after */

import {
  expect
}
from 'chai';
// import Developer from '../dist/developer.js';

describe('dirgen tests', () => {

  // var developer;
  //
  // before(() => developer = new Developer('Alex', '99'));
  //
  // after(() => developer = undefined);

  // it('getName', () => {
  //   expect(developer.getName()).to.equal('Alex');
  // });



});