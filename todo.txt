warnings and non-generated output check

queue up the can not write error messages

suppress the error messages on existing structures write attempts

message formatting for errors in writing or deleting

redo the overwrite test case, file safety test

new options in help for the force

verbose mode to allow for showing the errors and warnings

test deeply nested folder structures

Module
-module type support
-What should be checked in to the final npm repo?
-Selective check-ins for the dist folder:
  -not dist folder on git, but allow for npm?
  npmignore and gitignore

Backlog/Optimize
-- pluralizing the error messages
-- npm start more compatible with windows
-- programatic use should have object pass in
-- encoding type
-- permission level
-- step through
  -- overwrite
  -- warn [skip|ignore|walk]
